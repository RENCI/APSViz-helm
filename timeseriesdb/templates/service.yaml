apiVersion: v1
kind: Service
metadata:
  name: {{ include "apsviz-timeseriesdb.fullname" . }}
  labels:
    {{- include "apsviz-timeseriesdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apsviz-timeseriesdb.selectorLabels" . | nindent 4 }}
