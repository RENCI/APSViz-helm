apiVersion: v1
kind: Service
metadata:
  name: {{ include "apsviz-archiver.fullname" . }}
  labels:
    {{- include "apsviz-archiver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apsviz-archiver.selectorLabels" . | nindent 4 }}
