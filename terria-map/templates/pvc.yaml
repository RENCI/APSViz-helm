apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "apsviz-terriamap.name" . }}-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageSize }}
