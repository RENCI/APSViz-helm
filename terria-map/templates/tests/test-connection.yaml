apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apsviz-terriamap.fullname" . }}-test-connection"
  labels:
    {{- include "apsviz-terriamap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apsviz-terriamap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
