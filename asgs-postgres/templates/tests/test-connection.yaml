apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asgs-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "asgs-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asgs-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
