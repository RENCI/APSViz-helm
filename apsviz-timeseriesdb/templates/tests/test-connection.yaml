apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apsviz-timeseriesdb.fullname" . }}-test-connection"
  labels:
    {{- include "apsviz-timeseriesdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apsviz-timeseriesdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
